# Core pipeline
# NOTE: langchain 1.x is the current stable line. ragas 0.2.x needs the older
# langchain 0.3 line, but since ragas is optional (eval harness only), we pin
# the main dependencies to the 1.x line that ships with Python 3.14 wheels.
langchain>=1.2
langchain-anthropic>=1.6
langchain-community>=0.4
langchain-openai>=1.6
langchain-text-splitters>=1.1
rank-bm25>=0.2.2
faiss-cpu>=1.13
networkx>=3.4
fastapi>=0.115
uvicorn>=0.32
pydantic>=2.10
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.0          # required for BeautifulSoup's "xml" parser used by fetch_filings
tiktoken>=0.12
python-dotenv>=1.0
streamlit>=1.40

# Embeddings / reranking (pulls in torch; large download)
sentence-transformers>=2.7

# Evaluation harness (optional — uncomment if you want RAGAS metrics)
# ragas==0.2.15     # requires langchain<0.4; uncomment only if you pin langchain to 0.3.x
# datasets>=3.1
# scipy>=1.16

# India variant (BSE/NSE) — omit if only using the US/SEC EDGAR variant
# bse>=3.1
# pdfplumber>=0.11
# pytesseract       # uncomment if you need OCR for scanned-page PDFs (also needs system tesseract-ocr)
